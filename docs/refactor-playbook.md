# Refactor Playbook

## Current Status

- Date: 2026-03-12
- Phase: Foundation + guardrails + notifications decomposition + mobile shell decomposition + Websys POS integration surface
- Overall health: In progress (latest chunk validated)
- Progress snapshot:
  - Entry shells: `apps/mobile/App.tsx` `1` line and `apps/mobile-admin/App.tsx` `1` line (from combined `19020`, `99.99%` reduced)
  - Implementation shells moved to: `apps/mobile/MobileAppShell.tsx` (`768`) and `apps/mobile-admin/AdminAppShell.tsx` (`1018`)
  - Combined implementation shells: `1786` lines

## Completed Slices

| Slice                                               | What moved                                                                                     | Tests added                                                        | Notes                                                                               |
| --------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ | ----------------------------------------------------------------------------------- |
| Foundation guardrails                               | Max-line and no-direct-fetch scripts wired to root scripts                                     | Guard scripts executed locally                                     | Staged scope only to `admin/groups` and mobile shell caps                           |
| Web groups API wrapper                              | `admin/groups` pages now call shared client wrappers                                           | Existing behavior preserved (silent load failures, same submit UX) | First migrated UI scope with direct-fetch blocked                                   |
| Web offices + status API wrappers                   | `admin/offices` and `admin/status` now use shared wrappers                                     | Guard scripts executed with expanded scope                         | Preserved create/load behavior while removing direct-fetch from migrated pages      |
| Web time API wrapper                                | `admin/time` now uses shared time-admin wrapper module                                         | Full validation matrix run after migration                         | Preserved add/edit/delete flows and status messaging; removed direct-fetch          |
| Web users wrappers (list/new/search)                | `admin/users`, `admin/users/new`, and `admin/users/search` now use shared users-admin wrappers | Full validation matrix run after migration                         | Preserved toggle/archive/restore/permanent-delete and create flows                  |
| Web users wrapper (`[id]`)                          | `admin/users/[id]` now uses shared users-admin and time-admin wrappers                         | Full validation matrix run after migration                         | Preserved edit-user, archive-user, and manual time-entry flows                      |
| Web settings/company/dashboard wrappers             | `admin/settings`, `admin/company`, and `admin` dashboard now use shared wrappers               | Full validation matrix run after migration                         | Preserved save and load behavior; removed direct-fetch from migrated pages          |
| Web schedules/notifications/company-orders wrappers | `admin/schedules`, `admin/notifications`, and `admin/company-orders*` now use shared wrappers  | Full validation matrix run after migration                         | Preserved scheduling, messaging, and ordering behaviors while removing direct-fetch |
| Web notifications policy decomposition              | Split `admin/notifications/page.tsx` into hooks and focused panels; expanded browser controls for source toggles, repeated no-break cadence, and default device presets | API build + web build + mobile/mobile-admin typechecks + guard scripts + API tests | Restored handbook compliance for the notifications route while keeping browser-side notification management in one bounded feature module |
| Websys POS integration surface                      | Added a dedicated `/integrations/restaurant-pos` API adapter, reusable typed client/hooks package, POS handoff handbook, and owner-side `Websys POS Tenant` provisioning flag | Full validation matrix + package typecheck | Reused existing reports/schedules services instead of duplicating business logic; documented tenant-vs-office scope explicitly for payouts and exposed a persisted owner-managed POS tenant profile |
| Web owner wrappers                                  | `owner`, `owner/tenants`, and `owner-login` now use shared owner wrapper modules               | Full validation matrix run after migration                         | Preserved owner login, tenant CRUD, fallback feature handling, and export behavior  |
| Web reports core wrappers                           | `reports`, `audit`, `daily`, `hours`, `payroll`, `tips`, and `comparison/shared` use wrappers | Full validation matrix run after migration                         | Preserved report params, load/error behavior, and export links while removing fetch |
| Web reports hotspot wrappers                        | `reports/sales` and `reports/liquor-control` now use shared wrappers                            | Full validation matrix run after migration                         | Preserved high-complexity report workflows while removing direct-fetch in both pages |
| Web app shell/login wrappers                        | `admin-login` and root `app/page` now use shared wrappers                                       | Full validation matrix run after migration                         | Preserved login, tenant context, punch, and preloading behaviors while removing fetch |
| Mobile shell styles extraction                      | Moved `styles` blocks from `mobile/App.tsx` and `mobile-admin/App.tsx` into `App.styles.ts`    | Mobile typechecks + full validation matrix                         | No behavior change; reduced shell file size and tightened line-limit overrides       |
| Mobile runtime helpers extraction                   | Moved API runtime candidate/base64/pending-tips helpers into `api-runtime.ts` modules           | Mobile typechecks + full validation matrix                         | No behavior change; further reduced shell size and tightened line-limit overrides    |
| Mobile copy blocks extraction                       | Moved `mobile` i18n and `mobile-admin` copy dictionaries into dedicated modules                  | Mobile typechecks + full validation matrix                         | No behavior change; reduced shell file size again and tightened line-limit overrides |
| Mobile shared types extraction                      | Moved `mobile/App.tsx` and `mobile-admin/App.tsx` domain type declarations into `types.ts` files | Mobile typechecks + full validation matrix                         | No behavior change; reduced shell size and tightened line-limit overrides again      |
| Mobile admin helper extraction                      | Moved mobile-admin date/time/schedule/input helper functions into `app-helpers.ts`               | Mobile typechecks + full validation matrix                         | No behavior change; reduced admin shell size and tightened line-limit override again |
| Mobile helper extraction                            | Moved mobile clock/tips/order/date/input helper functions into `app-helpers.ts`                  | Mobile typechecks + full validation matrix                         | No behavior change; reduced mobile shell size and tightened line-limit override again |
| Mobile admin state helper extraction                | Moved mobile-admin default schedule/form/permission helpers into `app-state-helpers.ts`           | Mobile typechecks + full validation matrix                         | No behavior change; reduced admin shell size and tightened line-limit override again |
| Mobile admin navigation extraction                  | Moved mobile-admin static report/navigation constants into `app-navigation.ts`                    | Mobile typechecks + full validation matrix                         | No behavior change; reduced admin shell size and tightened line-limit override again |
| Mobile config extraction                            | Moved mobile shell action/storage/logo constants into `app-config.ts`                              | Mobile typechecks + full validation matrix                         | No behavior change; reduced mobile shell size and tightened line-limit override again |
| Mobile admin invoice helper extraction              | Moved mobile-admin invoice row normalization into `invoice-helpers.ts`                             | Mobile typechecks + full validation matrix                         | No behavior change; reduced admin shell size and tightened line-limit override again |
| Mobile admin draft/schedule state extraction        | Moved company-order draft and schedule day state-update logic into helper modules                  | Mobile typechecks + full validation matrix                         | No behavior change; reduced admin shell size and tightened line-limit override again |
| Mobile draft/invoice helper extraction              | Moved mobile company-order draft state and invoice row normalization into helper modules            | Mobile typechecks + full validation matrix                         | No behavior change; reduced mobile shell size and tightened line-limit override again |
| Mobile admin login component extraction             | Moved mobile-admin login card JSX into `components/LoginCard.tsx`                                   | Mobile typechecks + full validation matrix                         | No behavior change; reduced admin shell size and tightened line-limit override again |
| Mobile admin groups/reports component extraction    | Moved mobile-admin groups/reports JSX sections into dedicated components                             | Mobile typechecks + full validation matrix                         | No behavior change; reduced admin shell size and tightened line-limit override again |
| Mobile admin alerts component extraction            | Moved mobile-admin alerts JSX section into `components/AlertsCard.tsx`                               | Mobile typechecks + full validation matrix                         | No behavior change; reduced admin shell size and tightened line-limit override again |
| Mobile admin capture/schedules/company-orders extraction | Moved capture, schedules, and company-orders JSX sections into dedicated components                 | Mobile typechecks + guardrail checks                               | No behavior change; major `App.tsx` reduction while preserving handlers/routes         |
| Mobile admin offices component extraction           | Moved locations/geofence JSX section into `components/OfficesCard.tsx`                               | Mobile typechecks + guardrail checks                               | No behavior change; further reduced admin shell size and tightened line-limit override |
| Mobile admin dashboard component extraction         | Moved admin overview + manager shift + approvals JSX into `components/DashboardCard.tsx`             | Mobile typechecks + full validation matrix                         | No behavior change; further reduced admin shell size and tightened line-limit override |
| Mobile admin users-create section extraction        | Moved users header/create-user JSX into `components/UsersCreateSection.tsx`                           | Mobile typechecks + guardrail checks                               | No behavior change; reduced admin shell size while keeping edit/list behavior in shell |
| Mobile admin users/liquor workspace extraction      | Moved users edit/list and liquor inventory/catalog/operations/scans/invoices/analytics/activity JSX into dedicated components | Mobile typechecks + guardrail checks                               | No behavior change; major admin shell reduction while preserving event/state wiring |
| Mobile admin shell/view-model extraction            | Moved admin shell header/tab chrome and liquor-control view-model calculations into dedicated modules  | Mobile typechecks + guardrail checks                               | No behavior change; reduced `App.tsx` orchestration size and tightened line-limit override |
| Mobile admin liquor-control card extraction         | Moved liquor-control card composition from `App.tsx` into `components/LiquorControlCard.tsx`           | Mobile-admin typecheck + guardrail checks                          | No behavior change; `App.tsx` is now a thin composition wrapper for liquor-control UI |
| Mobile tenant bar extraction                        | Moved repeated tenant header bar into `components/TenantTopBar.tsx`                                   | Mobile typechecks + guardrail checks                               | No behavior change; removed duplicated branch UI and reduced root shell size |
| Mobile clock-station card extraction                | Moved clock station punch/tips form card from `App.tsx` into `components/ClockStationCard.tsx`         | Mobile typechecks + guardrail checks                               | No behavior change; reduced root shell size while preserving all punch/tip flows |
| Mobile team-status cards extraction                 | Moved `Working Now` and `Today's Team` cards into `components/WorkingNowCard.tsx` and `components/TodayTeamCard.tsx` | Mobile typechecks + guardrail checks                               | No behavior change; reduced root shell size and kept refresh/filter flows intact |
| Mobile last-punch card extraction                   | Moved `This Device` last-punch panel into `components/LastPunchCard.tsx`                                | Mobile typechecks + guardrail checks                               | No behavior change; reduced root shell size and preserved device punch summary view |
| Mobile company-order export controls extraction     | Moved company-order export format buttons into `components/CompanyOrderExportButtons.tsx`               | Mobile typechecks + guardrail checks                               | No behavior change; reduced root shell size and preserved export behavior/status |
| Mobile company-orders card extraction               | Moved supplier/search/items/cart/notes/recent-orders company-order card composition into `components/CompanyOrdersCard.tsx` | Mobile typechecks + guardrail checks                               | No behavior change; reduced root shell size while preserving all company-order handlers and state wiring |
| Mobile liquor-control card extraction               | Moved liquor-control composition (gating, invoice OCR, inventory rows, and recent scans) into `components/LiquorControlCard.tsx` | Mobile typechecks + guardrail checks                               | No behavior change; reduced root shell size while preserving all liquor handlers and state wiring |
| Mobile liquor payload normalizer extraction         | Moved liquor catalog/count/bottle-scan payload normalization from `mobile/App.tsx` into `liquor-control-normalizers.ts` | Mobile typechecks + full validation matrix                         | No behavior change; preserved parser defaults/type guards while shrinking root loader logic |
| Mobile tenant runtime extraction                    | Moved tenant employee-context fetch/normalization/office selection and tenant-resolve request logic from `mobile/App.tsx` into `tenant-context-runtime.ts` | Mobile typechecks + full validation matrix                         | No behavior change; preserved tenant validation, office persistence, and directory fallback behavior |
| Mobile active-shift/company-order helper extraction | Moved active-shift storage parser into `active-shift-storage.ts` and company-order supplier payload grouping into `company-order-draft-helpers.ts` | Mobile typechecks + guardrail checks                               | No behavior change; preserved shift hydration and company-order submit payload grouping behavior |
| Mobile company-order view helper extraction         | Moved supplier/item filtering, visibility, cart derivation, and summary counts from `mobile/App.tsx` into `company-order-view-helpers.ts` | Mobile typechecks + guardrail checks                               | No behavior change; preserved cart/search/show-more and summary count behavior |
| Mobile punch workflow helper extraction             | Moved punch payload construction/error classification into `punch-runtime.ts` and active-shift builder into `active-shift-storage.ts`; reused shared tips-submitted callback in `App.tsx` | Mobile typechecks + guardrail checks                               | No behavior change; preserved punch/tips validation branches, recovery behavior, and status messaging |
| Mobile entry-screen composition extraction          | Moved tenant loading/setup/location-selection entry states from `mobile/App.tsx` into `components/TerminalEntryScreens.tsx` | Mobile typechecks + full validation matrix                         | No behavior change; preserved tenant setup, loading locations, and required-location flows while thinning root shell composition |
| Mobile schedule selector extraction                 | Moved schedule shift-label formatting, role-tab/filter derivation, and schedule summary label builders from `mobile/App.tsx` into `schedule-view-helpers.ts` | Mobile typechecks + guardrail checks                               | No behavior change; preserved role filtering and localized start/end/any-time labels while shrinking root selector logic |
| Mobile workspace composition extraction             | Moved logged-in workspace card composition from `mobile/App.tsx` into `components/TerminalWorkspace.tsx` | Mobile typechecks + guardrail checks                               | No behavior change; preserved card ordering, handlers, and tab-gated rendering while thinning root app shell |
| Mobile today/working normalizer extraction          | Moved today-schedule response parsing and working-now row normalization/query-suffix building from `mobile/App.tsx` into `today-schedule-helpers.ts` and `working-now-helpers.ts` | Mobile typechecks + guardrail checks                               | No behavior change; preserved fallback/filter behavior and active-status normalization while shrinking loader logic |
| Mobile company-order catalog normalizer extraction  | Moved supplier/item catalog payload normalization from `mobile/App.tsx` into `company-order-catalog-normalizers.ts` | Mobile typechecks + guardrail checks                               | No behavior change; preserved supplier/item fallback trimming while shrinking company-order catalog loader logic |
| Mobile employee/today-schedule runtime extraction   | Moved office-scope fallback fetch flow for employees and today schedule from `mobile/App.tsx` into `employee-load-runtime.ts` and `today-schedule-runtime.ts` | Mobile typechecks + guardrail checks                               | No behavior change; preserved office-scope fallback semantics while reducing root loader complexity |
| Mobile liquor photo workflow extraction             | Moved bottle scan and invoice photo/analyze/apply workflow internals from `mobile/App.tsx` into `liquor-photo-workflows.ts` | Mobile typechecks + guardrail checks                               | No behavior change; preserved permissions/media/fetch flow and status outcomes while shrinking root workflow logic |
| Mobile liquor operation payload extraction          | Moved liquor catalog row update/count row validation and payload builders from `mobile/App.tsx` into `liquor-operation-payloads.ts` | Mobile typechecks + guardrail checks                               | No behavior change; preserved validation messages/payload shapes while reducing inline operation branching |
| Mobile company-order runtime extraction             | Moved company-order list query construction and orders payload normalization from `mobile/App.tsx` into `company-order-runtime.ts` | Mobile typechecks + guardrail checks                               | No behavior change; preserved order-list request params and week-start extraction behavior |
| Mobile tips runtime extraction                      | Moved tips work-date resolution, amount parsing, and already-submitted message matching from `mobile/App.tsx` into `tips-runtime.ts` | Mobile typechecks + guardrail checks                               | No behavior change; preserved tips validation branches and duplicate-submit handling |
| Mobile-admin liquor analytics export helper extraction | Moved large liquor analytics export generation/writer logic from `App.tsx` into `liquor-analytics-export.ts` | Mobile typechecks + guardrail checks                               | No behavior change; `App.tsx` now orchestrates status/state while helper owns export formatting/output |
| Mobile-admin company-order export helper extraction | Moved company-order export request/download logic from `App.tsx` into `company-order-export.ts` | Mobile typechecks + guardrail checks                               | No behavior change; `App.tsx` now orchestrates base selection/state while helper owns headers/download output |
| Mobile-admin liquor catalog assist helper extraction | Moved local smart ranking + remote/fallback AI assist flow from `App.tsx` into `liquor-catalog-assist.ts` | Mobile typechecks + guardrail checks                               | No behavior change; `App.tsx` now orchestrates guard/loading/state while helper owns matching/fallback logic |
| Mobile-admin liquor movement/quick-count payload helper extraction | Moved create-movement and quick-count validation/payload builders from `App.tsx` into `liquor-operation-payloads.ts` | Mobile typechecks + guardrail checks                               | No behavior change; `App.tsx` now orchestrates API/status/reset while helper owns payload validation rules |
| Mobile-admin liquor catalog payload helper extraction | Moved catalog-row update, inventory-row count, and catalog-create validation/payload builders from `App.tsx` into `liquor-catalog-payloads.ts` | Mobile typechecks + guardrail checks                               | No behavior change; `App.tsx` now orchestrates API/status/reset while helper owns catalog/count payload validation rules |
| Mobile-admin liquor UPC merge helper extraction     | Moved UPC lookup candidate-to-form merge logic from `App.tsx` into `liquor-upc-lookup.ts`         | Mobile typechecks + guardrail checks                               | No behavior change; preserved existing status strings and form merge behavior after lookup responses |
| Mobile-admin liquor photo workflows helper extraction | Moved bottle-scan capture/analyze and invoice photo/analyze/apply workflow plumbing from `App.tsx` into `liquor-photo-workflows.ts` | Mobile typechecks + guardrail checks                               | No behavior change; `App.tsx` keeps manager/premium/office guards and state transitions while helper owns shared media/fetch workflow steps |
| Mobile-admin inline localization extraction         | Moved inline message localization/regex + dictionary logic into `inline-message.ts`                | Mobile typechecks + guardrail checks                               | No behavior change; `App.tsx` now uses a thin inline localization callback |
| Mobile-admin company-order selector extraction      | Moved supplier/item filters, visibility logic, cart math, and supplier payload grouping into `company-order-view-helpers.ts` | Mobile typechecks + guardrail checks                               | No behavior change; preserved ordering, quantity totals, and submit payload structure |
| Mobile-admin schedule selector extraction           | Moved schedule role tabs/filtering/labels and pending override derivation into `schedule-state-helpers.ts` | Mobile typechecks + guardrail checks                               | No behavior change; preserved role filtering, fallback labels, and pending override data mapping |
| Mobile-admin today-schedule parser extraction       | Moved today-schedule API normalization + legacy endpoint fallback builder into `today-schedule-helpers.ts` | Mobile typechecks + guardrail checks                               | No behavior change; preserved current API parsing and 404 fallback behavior |
| Mobile-admin liquor-control normalizer extraction   | Moved liquor kinds/catalog/count/movement/scan payload normalization into `liquor-control-normalizers.ts` | Mobile typechecks + guardrail checks                               | No behavior change; preserved defaults/filters/type guards while shrinking root loader logic |
| Mobile-admin dev header runtime extraction          | Moved admin dev-header construction from `App.tsx` into `api-runtime.ts`                              | Mobile typechecks + guardrail checks                               | No behavior change; preserved admin request header values while shrinking root request wrapper logic |
| Mobile-admin tab visibility helper extraction       | Moved screen-tab permission gating/visible-tab filtering from `mobile-admin/App.tsx` into `tab-access-helpers.ts` | Mobile typechecks + guardrail checks                               | No behavior change; preserved tab access behavior for all permission combinations |
| Mobile-admin employee/location selector extraction  | Moved active location label, office geo target, schedule employee selection, and employee list filters into `employee-view-helpers.ts` | Mobile typechecks + guardrail checks                               | No behavior change; preserved office/schedule/message selection behavior while shrinking root selector logic |
| Mobile-admin access/active-now helper extraction    | Moved access-profile manager/session scope resolution and active-now row normalization/filtering from `mobile-admin/App.tsx` into `access-profile-helpers.ts` and `active-now-helpers.ts` | Mobile typechecks + guardrail checks                               | No behavior change; preserved manager session fallback and active-now status filtering while shrinking loader logic |
| Mobile-admin manager-session selector extraction    | Moved manager punch-status map, manager profile/row selection, and manager action derivation from `mobile-admin/App.tsx` into `manager-session-helpers.ts` | Mobile typechecks + guardrail checks                               | No behavior change; preserved manager punch state/action behavior while shrinking root selector logic |
| Mobile-admin loader/message helper extraction       | Moved employee/offices/notifications loader reconciliation + employee-message validation/payload + schedule-override status text derivation from `mobile-admin/App.tsx` into helper modules | Mobile typechecks + guardrail checks                               | No behavior change; preserved loader reconciliation and messaging flows while reducing inline branching |
| Mobile-admin company-order runtime extraction       | Moved company-order catalog supplier normalization/selection and order-list query + rows normalization from `mobile-admin/App.tsx` into `company-order-runtime.ts` | Mobile typechecks + guardrail checks                               | No behavior change; preserved company-order catalog/list loading behavior and selected-supplier fallback |
| Mobile-admin screen renderer extraction             | Replaced `switch`-based screen content selector with typed renderer map in `mobile-admin/App.tsx` | Mobile typechecks + guardrail checks                               | No behavior change; preserved per-screen render dispatch while reducing composition branching |
| Mobile-admin liquor-control runtime extraction      | Moved liquor-control feed/monthly/yearly query construction and target-cost query handling from `mobile-admin/App.tsx` into `liquor-control-runtime.ts` | Mobile typechecks + guardrail checks                               | No behavior change; preserved liquor report/count/movement/scan query semantics while reducing loader branching |
| Mobile-admin user/office payload extraction         | Moved edit-user form mapping/payload validation and office geofence payload validation/builders from `mobile-admin/App.tsx` into `user-edit-helpers.ts` and `office-geofence-helpers.ts` | Full validation matrix run after extraction                        | No behavior change; preserved edit-user and office/geofence validation messages and payload shapes while shrinking root imperative branches |
| Mobile-admin sales-capture helper extraction        | Moved sales report payload validation, expense payload validation, and receipt-asset normalization from `mobile-admin/App.tsx` into `sales-capture-helpers.ts` | Full validation matrix run after extraction                        | No behavior change; preserved capture/report validation messages and payload shapes while reducing root imperative branches |
| Mobile-admin report/manager runtime extraction      | Moved report query/result helpers and manager tips/punch payload+status helpers from `mobile-admin/App.tsx` into `report-runtime.ts` and `manager-punch-runtime.ts` | Full validation matrix run after extraction                        | No behavior change; preserved report validation strings, manager tips/punch status strings, and punch payload shapes while reducing root imperative branches |
| Mobile-admin today-schedule runtime extraction      | Moved today-schedule load + legacy fallback orchestration from `mobile-admin/App.tsx` into `today-schedule-runtime.ts` | Full validation matrix run after extraction                        | No behavior change; preserved today-schedule fallback trigger and status handling while reducing root async branching |
| Mobile-admin loader/runtime helper extraction       | Moved summary/employees/offices/groups/notifications loader flows, active-now loader, employee-message/schedule-override alert actions, group/schedule payload helpers, company-order loader reducers, and liquor-control snapshot loader orchestration from `mobile-admin/App.tsx` into runtime/helper modules | Full validation matrix run after extraction                        | No behavior change; preserved loader fallback/status behavior and payload shapes while reducing root async branching |
| Mobile-admin auth/push/access runtime extraction    | Moved tenant-directory admin auth flow across API base candidates, push-registration orchestration, and access-profile fetch/derivation from `mobile-admin/App.tsx` into `admin-auth-runtime.ts`, `push-runtime.ts`, and `access-profile-runtime.ts` | Full validation matrix run after extraction                        | No behavior change; preserved login error/status behavior, base resolution semantics, push-registration guard/permission handling, and access-profile session derivation behavior |
| Mobile-admin user/office runtime extraction         | Moved user create/disable/load/save-edit request orchestration and office create/geofence request orchestration from `mobile-admin/App.tsx` into `user-management-runtime.ts` and `office-runtime.ts` | Full validation matrix run after extraction                        | No behavior change; preserved user/office validation messages, payload shapes, and status outcomes while reducing root async branching |
| Mobile-admin company-order/schedule runtime extraction | Moved company-order submit/export request/status orchestration and employee-schedule load/save request orchestration from `mobile-admin/App.tsx` into `company-order-actions-runtime.ts` and `schedule-runtime.ts` | Validation matrix run (web build blocked by external `.next/lock`) | No behavior change; preserved company-order submission/export statuses and schedule load/save validation/status behavior while reducing root async branching |
| Mobile-admin request runtime thin-handler extraction | Moved group create, report run, sales save, sales-expense save, manager tips submit, manager self-punch, and force-punch request flows from `mobile-admin/App.tsx` into runtime/helper modules | Mobile/admin typechecks + guardrail checks + API build/tests | No behavior change; preserved validation/status strings while reducing root async request branching |
| Mobile-admin liquor actions runtime extraction | Moved liquor catalog/count/create-kind/delete-kind/movement/quick-count/UPC lookup request plumbing from `mobile-admin/App.tsx` into `liquor-control-runtime.ts` | Mobile/admin typechecks + guardrail checks + API build/tests | No behavior change; preserved access guards, status messages, and state-reset behavior while thinning action handlers |
| Mobile-admin app-effects extraction | Moved API health probe, data-load refresh effects, and visible-tab sync effects from `mobile-admin/App.tsx` into `data-load-effects.ts` | Mobile/admin typechecks + guardrail checks + API build/tests | No behavior change; preserved refresh cadence/trigger dependencies while reducing root effect density |
| Mobile-admin view-state hook extraction | Moved company-order derived selector/memo state and today-schedule/alerts derived selector state from `mobile-admin/App.tsx` into dedicated view-state hooks | Mobile/admin typechecks + guardrail checks + API build/tests | No behavior change; preserved list/search/show-more and schedule-role filtering behavior while shrinking root view orchestration |
| Mobile-admin action hook extraction | Moved company-order submit/draft/export handlers, manager punch handlers, and schedule update/save handlers from `mobile-admin/App.tsx` into `company-order-action-hooks.ts`, `manager-punch-action-hooks.ts`, and `schedule-action-hooks.ts` | Mobile/admin typechecks + guardrail checks + API build/tests | No behavior change; preserved existing status messages, loading-state transitions, and post-action refresh behavior while shrinking root app shell handlers |
| Mobile company-order action hook extraction | Moved company-order draft/add/step/remove, submit, and export handlers from `mobile/App.tsx` into `company-order-action-hooks.ts` | Mobile/admin typechecks + guardrail checks + API build/tests | No behavior change; preserved access guards, payloads, success/error statuses, and post-submit refresh behavior while shrinking mobile shell handlers |
| Mobile-admin user/office/report-capture action hook extraction | Moved user create/disable/edit handlers, office/group handlers, and report/capture handlers from `mobile-admin/App.tsx` into `user-management-action-hooks.ts`, `office-group-action-hooks.ts`, and `report-capture-action-hooks.ts` | Mobile/admin typechecks + guardrail checks + API build/tests | No behavior change; preserved payloads/status strings/loading transitions while reducing remaining root handler density |
| Mobile-admin liquor-control action hook extraction | Moved liquor-control load/update/save/create/scan/invoice/export orchestration from `mobile-admin/App.tsx` into `liquor-control-action-hooks.ts` | Mobile-admin typecheck + guardrail checks | No behavior change; preserved manager/premium guards, status strings, and post-action refresh semantics while shrinking root shell handlers |
| Mobile-admin screen composition wrapper extraction | Moved users/schedules/groups/offices/liquor screen wrapper composition out of `mobile-admin/App.tsx` into dedicated `components/*Screen.tsx` modules | Mobile-admin typecheck + guardrail checks | No behavior change; preserved existing card props, handlers, and refresh wiring while reducing root shell composition density |
| Mobile tenant logout action extraction | Added tenant logout action in top bar and moved tenant/session/storage reset orchestration from `mobile/App.tsx` into `tenant-logout-action-hooks.ts` | Mobile typecheck + guardrail checks | No behavior change for clocking/order flows; new explicit tenant logout returns app to tenant entry state |
| Mobile company-order view-state hook extraction | Moved supplier/search/filter/show-more/cart derived selectors from `mobile/App.tsx` into `company-order-view-state.ts` | Mobile typecheck + guardrail checks | No behavior change; preserved item visibility, cart totals, and selected supplier draft behavior while reducing root memo density |
| Mobile liquor sheet view-state extraction | Moved liquor sheet row derivation and draft-sync effect from `mobile/App.tsx` into `liquor-sheet-view-state.ts` | Mobile typecheck + guardrail checks | No behavior change; preserved latest-count row math and draft default hydration while shrinking root shell effects/selectors |
| Mobile punch/tips action hook extraction | Moved punch submit (including geofence/location fallback and manager message handling) and tips submit handlers from `mobile/App.tsx` into `punch-tip-action-hooks.ts` | Mobile/admin typechecks + guardrail checks + API build/tests | No behavior change; preserved punch/tips guards, status strings, retry/fallback behavior, and session persistence transitions while shrinking root shell handlers |
| Mobile-admin shell derived view-state extraction | Moved inline i18n/localized message helpers, schedule/alerts/employee derived selectors, visible-tab derivation, and shell theme/API label derivation from `mobile-admin/App.tsx` into `app-view-state-hooks.ts` (`useAdminShellViewState`) | Mobile-admin + mobile typechecks + guardrail checks | No behavior change; preserved rendered labels/filters/tab visibility/theme logic while reducing root shell orchestration density |
| Mobile tip-submission key dedupe | Moved duplicate tip submission key derivation from `mobile/App.tsx` and `workspace-session-view-state.ts` into shared `app-helpers.ts` utility | Mobile + mobile-admin typechecks + guardrail checks | No behavior change; preserved tip-submission lookup semantics while removing duplicate key logic |
| Mobile-admin data-load action hook extraction | Moved summary/employees/offices/groups/notifications/active-now/today-schedule/company-order loaders from `mobile-admin/App.tsx` into `admin-load-action-hooks.ts` (`useAdminLoadActions`) | Mobile-admin + mobile typechecks + guardrail checks | No behavior change; preserved loader reconciliation/status handling while reducing root async orchestration density |
| Mobile-admin alerts action hook extraction | Moved employee-message send flow and schedule-override decision orchestration from `mobile-admin/App.tsx` into `alerts-action-hooks.ts` (`useAlertsActions`) | Mobile-admin + mobile typechecks + guardrail checks | No behavior change; preserved validation/status text and refresh ordering while reducing root async alert-action branching |
| Mobile-admin auth action hook extraction | Moved login and access-profile orchestration from `mobile-admin/App.tsx` into `admin-auth-action-hooks.ts` (`useAdminAuthActions`) | Mobile-admin + mobile typechecks + guardrail checks | No behavior change; preserved auth fallback/base-resolution behavior, permission hydration, and login session setup while reducing root auth branching |
| Mobile-admin API action hook extraction | Moved `fetchJson` and company-order export API callback orchestration from `mobile-admin/App.tsx` into `admin-api-action-hooks.ts` (`useAdminApiActions`) | Mobile-admin + mobile typechecks + guardrail checks | No behavior change; preserved API base-resolution and export result handling while thinning root API callback logic |
| Mobile-admin push registration effect extraction | Moved push device registration callback/effect from `mobile-admin/App.tsx` into `admin-push-effects.ts` (`useAdminPushEffects`) | Mobile-admin + mobile typechecks + guardrail checks | No behavior change; preserved push token registration lifecycle and sync error propagation while reducing root side-effect density |
| Mobile-admin content router extraction | Moved `App.tsx` inline `render*` screen composition block into `components/AdminContentRouter.tsx` | Mobile-admin + mobile typechecks + guardrail checks | No behavior change; preserved screen routing/render order and prop wiring while reducing root composition density |
| Mobile-admin session action hook extraction | Moved office-scope query helper and full admin logout/session reset orchestration from `mobile-admin/App.tsx` into `admin-session-action-hooks.ts` | Mobile-admin + mobile typechecks + guardrail checks | No behavior change; preserved logout reset semantics and office-scoped query behavior while reducing root handler density |
| Mobile-admin view effects extraction | Moved screen-driven data load/reset effects, geofence draft sync effect, and liquor search/draft sync effects from `mobile-admin/App.tsx` into `admin-view-effects.ts` | Mobile-admin + mobile typechecks + guardrail checks | No behavior change; preserved effect triggers/reset semantics while reducing root side-effect density |
| Mobile entry-shell extraction | Converted `apps/mobile/App.tsx` and `apps/mobile-admin/App.tsx` into thin entry shells that re-export `MobileAppShell.tsx` and `AdminAppShell.tsx` | Mobile-admin + mobile typechecks + guardrail checks | No behavior change; preserved app entrypoints while making root app files thin composition shells |
| Mobile-admin app-state hook extraction | Moved full admin shell `useState` declarations from `AdminAppShell.tsx` into `admin-app-state.ts` (`useAdminAppState`) | Full validation matrix run after extraction | No behavior change; preserved initial values and setter wiring while centralizing shell state bootstrap |
| Mobile-admin shell effects extraction | Moved tenant persistence, notification listener refresh, and liquor fallback/bootstrap effects from `AdminAppShell.tsx` into `admin-shell-effects.ts` | Full validation matrix run after extraction | No behavior change; preserved effect triggers and side-effect ordering while reducing shell side-effect density |
| Mobile-admin content-props consolidation | Consolidated `AdminAppShell.tsx` router prop wiring to spread `useAdminAppState` values and pass only computed/action fields explicitly | Full validation matrix run after extraction | No behavior change; preserved router prop contract while reducing shell prop-wiring duplication and line count |
| Mobile app-state hook extraction | Moved mobile shell `useState` declarations from `MobileAppShell.tsx` into `mobile-app-state.ts` (`useMobileAppState`) | Full validation matrix run after extraction | No behavior change; preserved initial values and setters while making shell composition more focused |

## In Progress

- Current bounded context: Mobile shell decomposition
- Current extraction target: Continue extracting remaining dense orchestration from `apps/mobile-admin/AdminAppShell.tsx` and `apps/mobile/MobileAppShell.tsx`
- Known risks: implementation shell modules (`MobileAppShell.tsx`, `AdminAppShell.tsx`) remain dense and still rely on temporary line-limit overrides
- Temporary overrides in use: targeted hotspots in `guardrails.config.json` now document remaining oversized files outside the notifications slice

## Next Slice

- Next bounded context: Mobile shell decomposition
- Why this order: Web migration is functionally complete for direct-fetch wrapper scope; remaining structural risk is mobile shell size/complexity
- Exit criteria:
  - Move direct `fetch` calls into wrapper modules
  - Add migrated paths to `noDirectFetch.include`
  - Keep route/payload behavior unchanged

## Guardrail State

- Max lines default cap: `300`
- Guard config: `guardrails.config.json`
- Active no-direct-fetch scope:
  - `apps/web/src/app/admin/groups/**/*.tsx`
  - `apps/web/src/app/admin/offices/**/*.tsx`
  - `apps/web/src/app/admin/status/**/*.tsx`
  - `apps/web/src/app/admin/time/**/*.tsx`
  - `apps/web/src/app/admin/users/page.tsx`
  - `apps/web/src/app/admin/users/new/page.tsx`
  - `apps/web/src/app/admin/users/[id]/page.tsx`
  - `apps/web/src/app/admin/users/search/page.tsx`
  - `apps/web/src/app/admin/settings/page.tsx`
  - `apps/web/src/app/admin/company/page.tsx`
  - `apps/web/src/app/admin/page.tsx`
  - `apps/web/src/app/admin/schedules/page.tsx`
  - `apps/web/src/app/admin/notifications/page.tsx`
  - `apps/web/src/app/admin/company-orders/page.tsx`
  - `apps/web/src/app/admin/company-orders/catalog/page.tsx`
  - `apps/web/src/app/owner/page.tsx`
  - `apps/web/src/app/owner/tenants/page.tsx`
  - `apps/web/src/app/owner-login/page.tsx`
  - `apps/web/src/app/admin-login/page.tsx`
  - `apps/web/src/app/page.tsx`
  - `apps/web/src/app/reports/page.tsx`
  - `apps/web/src/app/reports/audit/page.tsx`
  - `apps/web/src/app/reports/daily/page.tsx`
  - `apps/web/src/app/reports/hours/page.tsx`
  - `apps/web/src/app/reports/payroll/page.tsx`
  - `apps/web/src/app/reports/tips/page.tsx`
  - `apps/web/src/app/reports/sales/page.tsx`
  - `apps/web/src/app/reports/liquor-control/page.tsx`
- `apps/web/src/app/reports/comparison/shared.ts`
- `apps/api/src/restaurant-pos-integration/restaurant-pos-integration.controller.ts`
- `apps/api/src/restaurant-pos-integration/restaurant-pos-integration.service.ts`
- `packages/restaurant-pos-client/src/index.ts`
- `packages/restaurant-pos-client/src/client.ts`
- `packages/restaurant-pos-client/src/hooks.ts`
- `packages/restaurant-pos-client/src/types.ts`

## Validation Matrix

Run this matrix after each chunk:

1. `npm --workspace apps/api run build`
2. `npm --workspace apps/web run build`
3. `npx tsc -p apps/mobile/tsconfig.json --noEmit`
4. `npx tsc -p apps/mobile-admin/tsconfig.json --noEmit`
5. `npm run check:lines`
6. `npm run check:no-direct-fetch`
7. `npm --workspace apps/api run test -- --runInBand`

## Handoff Notes

- Root mobile entry files are now thin shells (`apps/mobile/App.tsx` and `apps/mobile-admin/App.tsx` both `1` line) re-exporting `MobileAppShell.tsx` and `AdminAppShell.tsx`.
- Mobile-admin data-load, alerts, auth, API callback, push registration, session reset, and view-effect orchestration now live in dedicated hooks (`admin-load-action-hooks.ts`, `alerts-action-hooks.ts`, `admin-auth-action-hooks.ts`, `admin-api-action-hooks.ts`, `admin-push-effects.ts`, `admin-session-action-hooks.ts`, `admin-view-effects.ts`).
- Mobile-admin inline screen render router now lives in `apps/mobile-admin/components/AdminContentRouter.tsx`; root shell composition is further thinned with unchanged behavior.
- Mobile-admin shell-derived view state now lives in `apps/mobile-admin/app-view-state-hooks.ts` (`useAdminShellViewState`).
- Implementation shells now live at `apps/mobile/MobileAppShell.tsx` (`768`) and `apps/mobile-admin/AdminAppShell.tsx` (`1018`), combined `1786` lines.
- Mobile-admin shell state bootstrap now lives in `apps/mobile-admin/admin-app-state.ts` (`useAdminAppState`) and shell-local tenant/notification/liquor bootstrap effects now live in `apps/mobile-admin/admin-shell-effects.ts`.
- Mobile shell state bootstrap now lives in `apps/mobile/mobile-app-state.ts` (`useMobileAppState`).
- Mobile-admin user/office/group/report/capture handlers moved into dedicated action hooks; behavior stayed stable while root handlers were thinned.
- Mobile-admin liquor-control orchestration moved into `liquor-control-action-hooks.ts`; behavior remained stable while root handler density was reduced.
- Mobile-admin users/schedules/groups/offices/liquor screen composition moved into dedicated `components/*Screen.tsx` wrappers; `apps/mobile-admin/App.tsx` reduced to `2010` lines and override tightened to `2050`.
- Mobile shell decomposition has advanced to `apps/mobile/App.tsx` at `932` lines; override tightened to `1000`.
- Employee mobile tenant logout is now available from the workspace top bar with a dedicated logout action hook that clears tenant/session storage and returns to tenant entry.
- Employee mobile company-order and liquor sheet derived view state moved into dedicated hooks to reduce root memo/effect density.
- Scope expanded to `admin/offices` and `admin/status` with zero direct fetch in migrated paths.
- Resolved web build blocker by keeping `proxy.ts` and removing duplicate `middleware.ts`.
- Scope expanded to `admin/time` with shared wrapper module and no direct fetch in migrated page.
- Scope expanded to `admin/users` list/new/search with shared wrapper modules and no direct fetch in migrated pages.
- Scope expanded to `admin/users/[id]` with shared wrapper modules and no direct fetch in migrated page.
- Scope expanded to `admin/settings`, `admin/company`, and `admin` dashboard with shared wrapper modules and no direct fetch in migrated pages.
- Scope expanded to `admin/schedules`, `admin/notifications`, and `admin/company-orders*`; admin fetch count is now `0`.
- `apps/web/src/app/admin/notifications/page.tsx` is back to a thin route shell (`5` lines); the browser notification feature now lives in focused panels and hooks under `apps/web/src/app/admin/notifications/{components,hooks}`.
- Browser notification management now covers source-level generation toggles, repeated no-break cadence/count, default delivery presets for newly registered devices, device-wide category delivery, registered-device management, employee messaging, and the live admin inbox.
- Websys POS now has a dedicated integration surface under `/integrations/restaurant-pos` for connection bootstrap, hours, payroll, tips, schedule management, and tenant-wide payouts, plus a reusable typed client/hooks package, a standalone handoff handbook for the POS team, and an owner-side `Websys POS Tenant` profile to provision supported tenants consistently.
- Scope expanded to `owner`, `owner/tenants`, and `owner-login` with shared wrappers and no direct fetch in migrated owner pages.
- Scope expanded to core reports (`reports`, `audit`, `daily`, `hours`, `payroll`, `tips`, `comparison/shared`) with shared wrappers and no direct fetch in migrated report pages.
- Scope expanded to report hotspots (`reports/sales` and `reports/liquor-control`) with shared wrappers and no direct fetch in those pages.
- Scope expanded to `admin-login` and root `app/page` with shared wrappers and no direct fetch in those pages.
- Mobile and mobile-admin style blocks extracted into dedicated style modules; line-limit overrides tightened.
- Mobile and mobile-admin runtime API helper blocks extracted into dedicated modules; line-limit overrides tightened again.
- Mobile i18n and mobile-admin copy dictionaries extracted into dedicated modules; line-limit overrides tightened again.
- Mobile domain type declarations extracted into `apps/mobile/types.ts` and `apps/mobile-admin/types.ts`; line-limit overrides tightened again.
- Mobile-admin date/time/schedule/input helper functions extracted into `apps/mobile-admin/app-helpers.ts`; admin line-limit override tightened again.
- Mobile clock/tips/order/date/input helper functions extracted into `apps/mobile/app-helpers.ts`; mobile line-limit override tightened again.
- Mobile-admin default schedule/form/permission helpers extracted into `apps/mobile-admin/app-state-helpers.ts`; admin line-limit override tightened again.
- Mobile-admin static report/navigation constants extracted into `apps/mobile-admin/app-navigation.ts`; admin line-limit override tightened again.
- Mobile shell action/storage/logo constants extracted into `apps/mobile/app-config.ts`; mobile line-limit override tightened again.
- Mobile-admin invoice row normalization extracted into `apps/mobile-admin/invoice-helpers.ts`; admin line-limit override tightened again.
- Mobile-admin company-order draft and schedule day update logic extracted into helper modules; admin line-limit override tightened again.
- Mobile company-order draft state and invoice row normalization extracted into helper modules; mobile line-limit override tightened again.
- Mobile-admin login card JSX extracted into `apps/mobile-admin/components/LoginCard.tsx`; admin line-limit override tightened again.
- Mobile-admin groups and reports JSX extracted into `apps/mobile-admin/components/GroupsCard.tsx` and `apps/mobile-admin/components/ReportsCard.tsx`; admin line-limit override tightened again.
- Mobile-admin alerts JSX extracted into `apps/mobile-admin/components/AlertsCard.tsx`; admin line-limit override tightened again.
- Mobile-admin capture/schedules/company-orders JSX extracted into dedicated component modules; admin shell reduced significantly with no behavior changes.
- Mobile-admin offices/geofence JSX extracted into `apps/mobile-admin/components/OfficesCard.tsx`; admin shell reduced again.
- Mobile-admin dashboard JSX extracted into `apps/mobile-admin/components/DashboardCard.tsx`; admin shell reduced again.
- Mobile-admin users header/create-user JSX extracted into `apps/mobile-admin/components/UsersCreateSection.tsx`; admin shell reduced again.
- Admin mobile line-limit override tightened to `5850` after liquor-control card extraction (`App.tsx` now `5789` lines).
- Mobile shell line-limit override tightened to `3850` after company-order export controls extraction (`App.tsx` reached `3827` lines).
- Mobile company-orders card composition extracted into `apps/mobile/components/CompanyOrdersCard.tsx`; mobile shell reduced to `3540` lines and line-limit override tightened to `3600`.
- Mobile liquor-control composition extracted into `apps/mobile/components/LiquorControlCard.tsx`; mobile shell reduced to `3182` lines and line-limit override tightened to `3300`.
- Mobile-admin liquor analytics export logic extracted into `apps/mobile-admin/liquor-analytics-export.ts`; admin shell reduced to `5230` lines and line-limit override tightened to `5300`.
- Mobile-admin company-order export logic extracted into `apps/mobile-admin/company-order-export.ts`; admin shell reduced to `5118` lines and line-limit override tightened to `5150`.
- Mobile-admin liquor catalog assist flow extracted into `apps/mobile-admin/liquor-catalog-assist.ts`; admin shell reduced to `4980` lines and line-limit override tightened to `5000`.
- Mobile-admin liquor movement/quick-count validation and payload logic extracted into `apps/mobile-admin/liquor-operation-payloads.ts`; admin shell reduced to `4905` lines and line-limit override tightened to `4950`.
- Mobile-admin liquor catalog/count/create payload logic extracted into `apps/mobile-admin/liquor-catalog-payloads.ts`; admin shell reduced to `4842` lines while preserving existing validation messages/payload shapes.
- Mobile-admin UPC lookup merge logic extracted into `apps/mobile-admin/liquor-upc-lookup.ts`; admin shell reduced further while preserving existing status strings and merge behavior.
- Mobile-admin bottle-scan and invoice media workflows extracted into `apps/mobile-admin/liquor-photo-workflows.ts`; admin shell reduced to `4728` lines and line-limit override tightened to `4750`.
- Mobile-admin inline localization moved into `apps/mobile-admin/inline-message.ts`; admin shell dropped to `4410` lines while preserving translation strings/regex behavior.
- Mobile-admin company-order list/cart selectors and submit payload grouping moved into `apps/mobile-admin/company-order-view-helpers.ts`; admin shell dropped to `4315` lines with unchanged order/cart behavior.
- Mobile-admin schedule role tabs/filtering/labels and pending-override derivation moved into `apps/mobile-admin/schedule-state-helpers.ts`; behavior preserved and shell reduced further.
- Mobile-admin today-schedule normalization and legacy fallback builder moved into `apps/mobile-admin/today-schedule-helpers.ts`; fallback behavior preserved and shell reduced to `4169` lines.
- Mobile-admin liquor kinds/catalog/count/movement/scan normalizers moved into `apps/mobile-admin/liquor-control-normalizers.ts`; shell reduced to `3901` lines.
- Mobile-admin dev-header construction moved into `apps/mobile-admin/api-runtime.ts`; shell reduced to `3892` lines.
- Mobile-admin line-limit override tightened to `3900` after helper extraction (`App.tsx` now `3892` lines).
- Mobile liquor catalog/count/scan normalizers moved into `apps/mobile/liquor-control-normalizers.ts`; shell reduced to `3098` lines.
- Mobile tenant context fetch/resolve runtime moved into `apps/mobile/tenant-context-runtime.ts`; shell reduced to `2951` lines.
- Mobile active-shift parser moved into `apps/mobile/active-shift-storage.ts` and company-order supplier payload grouping moved into `apps/mobile/company-order-draft-helpers.ts`; shell reduced to `2898` lines.
- Mobile company-order supplier/item selector and cart summary derivation moved into `apps/mobile/company-order-view-helpers.ts`; shell reduced to `2863` lines.
- Mobile punch payload/error helpers moved into `apps/mobile/punch-runtime.ts` and active-shift builder moved into `apps/mobile/active-shift-storage.ts`; shell reduced to `2841` lines.
- Mobile tenant loading/setup/location-selection entry states moved into `apps/mobile/components/TerminalEntryScreens.tsx`; shell reduced to `2777` lines.
- Mobile line-limit override tightened to `2800` after helper extraction (`App.tsx` now `2777` lines).
- Mobile schedule selectors/labels moved into `apps/mobile/schedule-view-helpers.ts`; shell reduced to `2746` lines with no behavior change.
- Mobile logged-in workspace composition moved into `apps/mobile/components/TerminalWorkspace.tsx`; shell reduced to `2676` lines and line-limit override tightened to `2700`.
- Mobile today-schedule and working-now normalizers moved into `apps/mobile/today-schedule-helpers.ts` and `apps/mobile/working-now-helpers.ts`; shell reduced to `2601` lines and line-limit override tightened to `2620`.
- Mobile company-order catalog payload normalization moved into `apps/mobile/company-order-catalog-normalizers.ts`; shell reduced to `2562` lines and line-limit override tightened to `2580`.
- Mobile employee and today-schedule office-scope fallback fetch flows moved into `apps/mobile/employee-load-runtime.ts` and `apps/mobile/today-schedule-runtime.ts`; shell reduced to `2514` lines.
- Mobile bottle-scan and invoice photo/analyze/apply workflows moved into `apps/mobile/liquor-photo-workflows.ts`; shell reduced to `2442` lines and line-limit override tightened to `2460`.
- Mobile liquor catalog/count operation validation and payload builders moved into `apps/mobile/liquor-operation-payloads.ts`; shell reduced to `2428` lines and line-limit override tightened to `2440`.
- Mobile company-order list query construction + orders payload normalization moved into `apps/mobile/company-order-runtime.ts`; shell reduced to `2427` lines.
- Mobile tips date/amount/error-match helpers moved into `apps/mobile/tips-runtime.ts`; shell reduced to `2425` lines and line-limit override tightened to `2430`.
- Mobile-admin tab permission gating moved into `apps/mobile-admin/tab-access-helpers.ts`; shell reduced while preserving tab behavior.
- Mobile-admin office/employee selector logic moved into `apps/mobile-admin/employee-view-helpers.ts`; shell reduced to `3869` lines and line-limit override tightened to `3880`.
- Mobile-admin access-profile and active-now row helpers moved into `apps/mobile-admin/access-profile-helpers.ts` and `apps/mobile-admin/active-now-helpers.ts`; shell reduced to `3852` lines and line-limit override tightened to `3860`.
- Mobile-admin manager punch/session selector logic moved into `apps/mobile-admin/manager-session-helpers.ts`; shell reduced to `3841` lines and line-limit override tightened to `3850`.
- Mobile-admin loader reconciliation helpers moved into `apps/mobile-admin/admin-loader-helpers.ts`; office/employee/notification loader behavior preserved with reduced inline branching.
- Mobile-admin employee-message validation/payload and schedule-override status text derivation moved into `apps/mobile-admin/employee-message-helpers.ts` and `apps/mobile-admin/schedule-override-helpers.ts`; shell reduced to `3832` lines and line-limit override tightened to `3840`.
- Mobile-admin company-order catalog/list runtime helpers moved into `apps/mobile-admin/company-order-runtime.ts`; shell reduced to `3828` lines.
- Mobile-admin render-content dispatch now uses typed screen renderer map; shell reduced to `3824` lines and line-limit override tightened to `3830`.
- Mobile-admin liquor-control feed/monthly/yearly query + target-cost query builders moved into `apps/mobile-admin/liquor-control-runtime.ts`; shell reduced to `3809` lines and line-limit override tightened to `3815`.
- Mobile-admin edit-user details mapping + payload validation moved into `apps/mobile-admin/user-edit-helpers.ts`; edit-user request validation/messages/payload shape preserved while reducing root inline branching.
- Mobile-admin office create/geofence validation + payload builders moved into `apps/mobile-admin/office-geofence-helpers.ts`; geofence validation messages/payload shape preserved while reducing root inline branching.
- Mobile-admin sales report/expense payload validation and receipt asset normalization moved into `apps/mobile-admin/sales-capture-helpers.ts`; capture/report validation messages/payload shape preserved while reducing root inline branching.
- Mobile-admin report query/result derivation moved into `apps/mobile-admin/report-runtime.ts`; report date validation strings and generated-row status text preserved while reducing root inline branching.
- Mobile-admin manager tips/punch payload and status derivation moved into `apps/mobile-admin/manager-punch-runtime.ts`; manager tip/punch validation strings and payload shapes preserved while reducing root inline branching.
- Mobile-admin today-schedule load + legacy fallback orchestration moved into `apps/mobile-admin/today-schedule-runtime.ts`; fallback trigger/status behavior preserved while reducing root async branching and tightening admin shell override to `3635` (`App.tsx` now `3625` lines).
- Mobile-admin active-now loader orchestration moved into `apps/mobile-admin/active-now-runtime.ts`; recent-punch normalization + active-now filtering behavior preserved while reducing root async branching.
- Mobile-admin group/schedule/company-order/liquor loader orchestration moved into `apps/mobile-admin/group-runtime.ts`, `apps/mobile-admin/schedule-runtime.ts`, `apps/mobile-admin/company-order-runtime.ts`, and `apps/mobile-admin/liquor-control-runtime.ts`; payload/query/status behavior preserved.
- Mobile-admin summary/employees/offices/groups/notifications loader flows and alerts actions moved into `apps/mobile-admin/admin-data-runtime.ts` and `apps/mobile-admin/alerts-runtime.ts`; data-sync/alert status behavior preserved while tightening admin shell override to `3545` (`App.tsx` now `3538` lines).
- Mobile-admin tenant-directory auth flow and push-registration orchestration moved into `apps/mobile-admin/admin-auth-runtime.ts` and `apps/mobile-admin/push-runtime.ts`; login/push guard behavior preserved while tightening admin shell override to `3485` (`App.tsx` now `3475` lines).
- Mobile-admin access-profile fetch/session derivation orchestration moved into `apps/mobile-admin/access-profile-runtime.ts`; access-permission/session scope behavior preserved while tightening admin shell override to `3475` (`App.tsx` now `3467` lines).
- Mobile-admin user create/disable/load/save-edit request orchestration moved into `apps/mobile-admin/user-management-runtime.ts`; user validation/status behavior preserved while reducing root branching.
- Mobile-admin office create/geofence request orchestration moved into `apps/mobile-admin/office-runtime.ts`; geofence validation/status behavior preserved while reducing root branching.
- Mobile-admin company-order submit/export and schedule load/save request orchestration moved into `apps/mobile-admin/company-order-actions-runtime.ts` and `apps/mobile-admin/schedule-runtime.ts`; status/validation behavior preserved while tightening admin shell override to `3390` (`App.tsx` now `3388` lines).
- Mobile-admin group/report/sales/manager request handlers moved into runtime modules (`group-runtime.ts`, `report-runtime.ts`, `sales-capture-helpers.ts`, `manager-punch-runtime.ts`); validation/status strings preserved while reducing root async branching.
- Mobile-admin liquor action request plumbing moved into `liquor-control-runtime.ts`; catalog/count/kind/movement/UPC request behavior and status outcomes preserved.
- Mobile-admin shared API request wrapper moved into `api-runtime.ts` (`fetchAdminApiJson`) and reused by `App.tsx`; request headers/base failover/error behavior preserved.
- Mobile-admin data-load and health/tab effects moved into `data-load-effects.ts`; refresh cadence and dependency triggers preserved.
- Mobile-admin company-order and schedule/alert derived view selectors moved into `company-order-view-state.ts` and `app-view-state-hooks.ts`; list/filter/show-more and role/override derivations preserved.
- Mobile-admin geofence draft mapping moved into `office-runtime.ts` (`resolveOfficeGeofenceDraftValues`) and liquor sheet draft sync moved into `liquor-control-view-helpers.ts`; UI field defaults preserved.
- Mobile-admin company-order draft/submit/export handlers moved into `company-order-action-hooks.ts`; order-item stepping and submit/export status behavior preserved with unchanged API payloads.
- Mobile-admin manager tips/self-punch/force-punch handlers moved into `manager-punch-action-hooks.ts`; pending-tip gating and punch status behavior preserved.
- Mobile-admin schedule load/update/save handlers moved into `schedule-action-hooks.ts`; schedule validation/status behavior preserved.
- Admin shell override tightened from `3105` to `2860` after latest extraction batch (`App.tsx` now `2828` lines).
- Mobile company-order draft/add/step/remove + submit/export handlers moved into `apps/mobile/company-order-action-hooks.ts`; order payload/status behavior preserved while reducing `apps/mobile/App.tsx` to `2329` lines and tightening mobile override to `2350`.
- Latest validation status for this chunk: API build + mobile typechecks + guardrails + API tests pass; web build is intermittently blocked by external `.next/lock` contention from system/file-provider processes in this environment.
- Additional validation note: `apps/mobile-owner/tsconfig.json` is not present in this workspace, so owner-mobile typecheck is not applicable to this repo layout.
- Guardrails remain intentionally staged to avoid blocking unrelated legacy areas.
- Continue expanding scope slice by slice and tighten overrides each sprint.
